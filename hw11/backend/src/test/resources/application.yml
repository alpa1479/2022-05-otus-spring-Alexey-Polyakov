spring:
  mongodb:
    embedded:
      version: 4.4.11
  data:
    mongodb:
      host: localhost
      port: 8081
      database: library

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              ReactiveMongoTemplate: DEBUG